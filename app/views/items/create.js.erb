<% if @item.valid? %>
   $('#items-table').append("<%= escape_javascript(render partial: 'items/item', locals: {item: @item}) %>");
 <% else %>
   $('.new-item').html("<%= escape_javascript(render partial: 'items/form', locals: { user: @user, item: @item }) %>");
 <% end %>
